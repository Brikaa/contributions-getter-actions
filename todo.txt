TODO
    - Configurable sort?
    - Repos ignore?
    - Script to generate the GitHub env format (DRY in action.yml, main.yml)
    - Use monthsInterval, token, username in mock objects to verify they are passed correctly
